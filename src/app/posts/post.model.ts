// defines what an object looks like, but can't instantiate new object
// force a certain object to look like something
export interface Post {
    id: string;
    title: string;
    content: string;
}